import { FiltraPorEtapaPipe } from './filtra-por-etapa.pipe';

describe('FiltraPorEtapaPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltraPorEtapaPipe();
    expect(pipe).toBeTruthy();
  });
});
